{
  "name": "photobrief-router",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bundle:check": "wrangler deploy --dry-run --outdir ../../dist-worker-check/workers/router"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250410.0",
    "wrangler": "4.14.0"
  }
}
