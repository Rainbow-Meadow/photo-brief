# PhotoBrief product spotlight video

The production demo video is generated from the Remotion composition in `remotion/src`.

Run locally:

```bash
cd remotion
npm install
npm run render
```

The GitHub Actions workflow `Render product demo video` regenerates `public/marketing/photobrief-demo.mp4` after Remotion changes land on `main`.
