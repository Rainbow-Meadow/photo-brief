name: CI Diagnostics

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/ci-diagnostics.yml

permissions:
  contents: write

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Run checks and capture logs
        shell: bash
        run: |
          set +e
          mkdir -p .ci-diagnostics
          {
            echo "# CI diagnostics"
            echo
            echo "Commit: $(git rev-parse HEAD)"
            echo "Run started: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo
          } > .ci-diagnostics/report.md

          run_check() {
            local name="$1"
            shift
            echo "## ${name}" >> .ci-diagnostics/report.md
            echo '```text' >> .ci-diagnostics/report.md
            "$@" > .ci-diagnostics/${name}.log 2>&1
            local code=$?
            cat .ci-diagnostics/${name}.log >> .ci-diagnostics/report.md
            echo '```' >> .ci-diagnostics/report.md
            echo >> .ci-diagnostics/report.md
            echo "${name}=${code}" >> .ci-diagnostics/status.env
            return 0
          }

          run_check npm-ci npm ci
          run_check typecheck npm run typecheck
          run_check lint npm run lint
          run_check build npm run build

          echo "## Summary" >> .ci-diagnostics/report.md
          cat .ci-diagnostics/status.env >> .ci-diagnostics/report.md

      - name: Commit diagnostics report
        shell: bash
        run: |
          rm -f .github/workflows/ci-diagnostics.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .ci-diagnostics .github/workflows/ci-diagnostics.yml
          git commit -m "Add CI diagnostics report" || echo "No diagnostics changes to commit"
          git push

# trigger: 2026-05-05T03:38:00Z
